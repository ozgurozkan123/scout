{
  "rewrites": [{ "source": "/(.+)", "destination": "/api/server" }],
  "functions": {
    "api/server.ts": {
      "maxDuration": 60
    }
  },
  "installCommand": "pnpm install --no-frozen-lockfile"
}
